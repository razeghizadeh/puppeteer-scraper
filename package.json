{
  "name": "scraper-service",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "postinstall": "apt-get update && apt-get install -y chromium-browser && npx playwright install --with-deps",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^20.0.0"
  }
}
